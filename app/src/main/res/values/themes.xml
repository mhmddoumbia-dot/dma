<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Thème de base ; le contenu réel est piloté par Jetpack Compose (ui/theme/Theme.kt). -->
    <style name="Theme.GestionFinances" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/finance_green</item>
        <item name="android:windowBackground">@color/finance_white</item>
    </style>
</resources>
